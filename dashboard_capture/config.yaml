name: "Dashboard Capture"
version: "1.0.3"
slug: "dashboard_capture"
description: "Captures a screenshot of the HA dashboard and saves it to /config/www"
arch:
  - amd64
  - aarch64
init: false
map:
  - config:rw
options:
  url: "http://homeassistant.local:8123/tv-display/0"
  interval: 10
  budget: 10000
schema:
  url: str
  interval: int
  budget: int